# 第17A步 正式测试环境收口记录

## 当前正式测试地址

- 管理后台：https://admin.gxkjy.com/
- 后端健康检查：https://api.gxkjy.com/api/health
- 扫码公开页：https://cert.gxkjy.com/c/{publicToken}
- 正常合格证示例：https://cert.gxkjy.com/c/f600b41e76e1c4504aacbc8a13de3344
- 作废合格证示例：https://cert.gxkjy.com/c/9ea7a4e2031482df5eccdb2b92959de9
- 新开证示例：https://cert.gxkjy.com/c/fd605e5da6013fa2821632c5d4d8f9da

## 临时地址和敏感信息检查

检查范围包括 `backend`、`admin-web`、`miniapp`、`public-h5`、`deploy`、`docs`、`scripts`、`.env.example`、`README.md`。

检查结论：

- 生产访问地址已经切换为 `https://api.gxkjy.com/api`、`https://admin.gxkjy.com/`、`https://cert.gxkjy.com`。
- `admin-web` 正式构建通过 `VITE_API_BASE_URL` 读取接口地址，正式部署使用 HTTPS 域名。
- `public-h5` 正式构建通过 `VITE_API_BASE_URL` 读取接口地址，正式部署使用 HTTPS 域名。
- `miniapp` 构建时通过 `VITE_API_BASE_URL` 读取接口地址，后续微信认证通过后需配置合法请求域名。
- `localhost` 和 `127.0.0.1` 仍存在于本地开发配置、Docker 内部端口绑定、数据库连接示例中，属于必要配置。
- `http://182.92.75.122` 仍存在于历史临时 IP 脚本和旧测试说明文档中，保留为历史排查记录，不作为正式测试入口。
- `xxx.com` 仍存在于 `.env.example`、部署模板和通用部署说明中，作为占位示例保留。
- 未发现应提交到代码中的真实 MySQL 密码、JWT 密钥、OSS 密钥或厂家接入密钥。

## 本次完成内容

- 新增后台 CSV 导出接口：
  - `GET /api/admin/companies/export`
  - `GET /api/admin/devices/export`
  - `GET /api/admin/detection-records/export`
  - `GET /api/admin/certificates/export`
  - `GET /api/admin/products/export`
  - `GET /api/admin/operation-logs/export`
- 后台页面新增导出按钮：
  - 企业管理
  - 设备管理
  - 检测记录
  - 合格证管理
  - 产品库
  - 操作日志
- CSV 文件统一使用 UTF-8 BOM，便于 Windows Excel 打开中文。
- 导出内容受当前筛选条件影响。
- 导出内容不包含密码哈希、JWT、OSS 密钥、厂家接入密钥、客户登录密码、内部 token 等敏感字段。
- 新增正式 HTTPS 冒烟测试脚本 `scripts/smoke-test-https-env.mjs`。
- 新增证书到期检查脚本 `scripts/check-cert-expiry.mjs`。
- 新增正式测试环境访问、导出、运维、小程序待办文档。

## 本次未做内容

- 未发布微信小程序，原因是微信认证尚未完成。
- 未配置微信小程序后台合法域名，待认证完成后由管理员手动配置。
- 未接入真实检测仪器，仍使用测试检测数据能力演示主流程。
- 未接入真实打印机，仍为模拟打印。
- 未上传 OSS 真实密钥，文件上传仍按后续计划处理。
- 未开放 MySQL 3306 或后端 3000 到公网。

## 数据库变更

本次不新增数据库表和字段，不需要 Prisma migration。

## 部署和验证

本次涉及后端和管理后台，需要重新构建并部署 `backend`、`admin-web`。`public-h5` 和 `miniapp` 需要本地构建验证，但无业务代码部署改动。
