# 谷芯快检云测试环境部署检查清单

## 1. 基础资源

- [ ] 阿里云 ECS 已准备。
- [ ] ECS 安全组已开放 `22`、`80`、`443`。
- [ ] ECS 安全组未对公网开放 MySQL `3306`。
- [ ] `api.xxx.com` 已解析到 ECS。
- [ ] `admin.xxx.com` 已解析到 ECS。
- [ ] `cert.xxx.com` 已解析到 ECS。
- [ ] Docker 已安装。
- [ ] Docker Compose 插件已安装。

## 2. 环境变量

- [ ] 已复制 `.env.example` 为 `.env`。
- [ ] `NODE_ENV=production`。
- [ ] `DATABASE_URL` 已配置。
- [ ] `DATABASE_URL_DOCKER` 已配置，并使用 `mysql` 作为容器内数据库主机名。
- [ ] `MYSQL_DATABASE` 已配置。
- [ ] `MYSQL_USER` 已配置。
- [ ] `MYSQL_PASSWORD` 已替换为强密码。
- [ ] `MYSQL_ROOT_PASSWORD` 已替换为强密码。
- [ ] `JWT_SECRET` 已替换为高强度随机字符串。
- [ ] `CERT_PUBLIC_BASE_URL=https://cert.xxx.com` 已配置。
- [ ] `PUBLIC_CERT_BASE_URL=https://cert.xxx.com` 已配置。
- [ ] `VITE_API_BASE_URL=https://api.xxx.com/api` 已配置。
- [ ] `VITE_CERT_PUBLIC_BASE_URL=https://cert.xxx.com` 已配置。
- [ ] OSS Bucket、Region、Endpoint、AccessKey 已配置。
- [ ] `.env` 未提交代码仓库。

## 3. 安全项

- [ ] 默认 `admin / Guxin@2026` 密码已修改。
- [ ] `ADMIN_INIT_PASSWORD` 已替换为部署密码。
- [ ] OSS AccessKey 未写入代码。
- [ ] HTTPS 证书私钥未提交代码仓库。
- [ ] 后台管理域名未随意公开传播。
- [ ] 测试环境和正式环境密钥分开。

## 4. HTTPS 和 Nginx

- [ ] `api.xxx.com` HTTPS 证书已放入 `deploy/nginx/certs`。
- [ ] `admin.xxx.com` HTTPS 证书已放入 `deploy/nginx/certs`。
- [ ] `cert.xxx.com` HTTPS 证书已放入 `deploy/nginx/certs`。
- [ ] `deploy/nginx/default.conf` 已从 `https.template.conf` 调整为真实域名。
- [ ] HTTP 可自动跳转 HTTPS。
- [ ] `/api` 请求可代理到 backend。
- [ ] 管理后台 history 路由刷新不 404。
- [ ] 扫码公开页 `/c/{publicToken}` 刷新不 404。

## 5. 数据库

- [ ] MySQL 容器已启动。
- [ ] MySQL 使用持久化 volume。
- [ ] Prisma migration 已执行。
- [ ] seed 已执行。
- [ ] seed 重复执行不会重复创建 admin。
- [ ] `/api/health` 返回 `database: ok`。

## 6. 前端和小程序

- [ ] admin-web 已构建。
- [ ] public-h5 已构建。
- [ ] miniapp 已构建到 `mp-weixin`。
- [ ] 微信小程序后台已配置合法请求域名 `https://api.xxx.com`。
- [ ] 小程序体验版可访问测试环境 API。
- [ ] 当前小程序仍为模拟打印，真实蓝牙打印后置。

## 7. 备份

- [ ] MySQL 备份脚本可手动执行。
- [ ] crontab 已配置每日自动备份。
- [ ] 备份保留天数已确认，例如 14 天。
- [ ] 至少完成一次备份恢复测试。

## 8. 第一版范围确认

- [ ] 已确认真实检测仪器对接后置。
- [ ] 已确认真实检测数据上传接口后置。
- [ ] 已确认真实打印后置。
- [ ] 已确认优博讯 K329 SDK 后置。
- [ ] 已确认不做 Excel 导出。
- [ ] 已确认不做复杂权限和复杂统计大屏。
- [ ] 已确认后台不做删除数据功能。

## 9. 部署后冒烟测试

- [ ] `https://api.xxx.com/api/health` 正常。
- [ ] `https://admin.xxx.com/login` 可打开。
- [ ] 后台管理员可登录。
- [ ] 可新增企业。
- [ ] 可创建企业账号。
- [ ] 可新增厂家接口和设备。
- [ ] 可生成测试检测记录。
- [ ] 小程序可登录企业账号。
- [ ] 小程序可生成合格证。
- [ ] `https://cert.xxx.com/c/{publicToken}` 可打开。
- [ ] 后台可查看检测记录和合格证。
