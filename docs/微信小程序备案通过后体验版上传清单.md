# 微信小程序备案通过后体验版上传清单

## 当前状态

1. 微信认证已通过。
2. 小程序备案状态：待备案通过后补充（当前已具备上传条件）。
3. 代码已上传微信公众平台（体验版），AppID：`wx396e186d8eab8382`。
4. 小程序产物接口地址已确认为正式域名：`https://api.gxkjy.com/api`（无 localhost 残留）。
5. Web 三端 + 后端已在腾讯云服务器用正式域名运行（api/admin/cert/gxkjy.com 均 200）。
6. 当前系统仍是正式测试环境，真实检测仪器和真实打印机尚未接入。

## 备案通过前可以准备

1. 准备小程序 AppID，填入微信开发者工具项目配置。
2. 检查小程序接口地址是否为：`https://api.gxkjy.com/api`。
3. 检查代码中不要残留临时 IP、localhost、调试 token、密码等敏感输出。
4. 本地执行小程序构建，确认 `miniapp/dist/build/mp-weixin` 可以生成。
5. 准备体验版测试账号：`demo / Demo@2026`。

## 构建正式产物（已就绪）

- 本地调试用 `miniapp/.env` 写的是 `http://localhost:3000/api`，日常 `pnpm dev` 不受影响。
- 上传前必须打**正式域名版本**产物，不要直接用普通 `build`（会带 localhost）：
  - 一键脚本：`pnpm build:mp-weixin:prod`（已加入 `miniapp/package.json`，内部用 `VITE_API_BASE_URL=https://api.gxkjy.com/api` 覆盖）。
  - 或手动：`VITE_API_BASE_URL=https://api.gxkjy.com/api pnpm build:mp-weixin`。
- 产物校验：构建后确认 `dist/build/mp-weixin/config/index.js` 内为 `https://api.gxkjy.com/api`。

## 备案通过后先做体验版

1. 在微信公众平台配置 request 合法域名：`https://api.gxkjy.com`。
2. 如后续小程序内打开扫码页，再配置 web-view 业务域名：`https://cert.gxkjy.com`。
3. 使用微信开发者工具导入目录：`miniapp/dist/build/mp-weixin`（用 prod 脚本构建的正式产物）。
4. 填写自己的小程序 AppID。
5. 上传体验版。
6. 添加体验成员。
7. 用体验版测试账号登录：`demo / Demo@2026`。
8. 验证检测记录、开具合格证、合格证详情、模拟打印等主流程。
9. 当前真实检测仪器未接入，检测数据仍以后台测试数据为主。
10. 当前真实打印机未接入，打印仍为模拟打印，仅用于演示验证。

## 暂不做

1. 不上传正式版。
2. 不提交正式审核。
3. 不接入真实蓝牙打印。
4. 不接入优博讯 K329 SDK。
5. 不接入真实检测仪器厂家接口。

## 备案通过后还需补充（证书）

当前 nginx 证书 SAN 仅覆盖 `api.gxkjy.com`，`admin.gxkjy.com` / `cert.gxkjy.com` 暂复用该证书（功能可用，但浏览器有安全提示）。备案通过后需：
1. 为 `api.gxkjy.com`、`admin.gxkjy.com`、`cert.gxkjy.com` 签发包含三域名的正式证书（如 Let's Encrypt 多 SAN / 通配符）。
2. 替换 `deploy/nginx/certs/` 下证书，并同步更新 `deploy/nginx/default.conf` 中三个 443 server 块的证书路径。
3. 重启 nginx 复测各域名无安全警告。

## 提醒

体验版测试通过后，再根据页面、文案、备案状态和客户反馈决定是否进入正式提审。
